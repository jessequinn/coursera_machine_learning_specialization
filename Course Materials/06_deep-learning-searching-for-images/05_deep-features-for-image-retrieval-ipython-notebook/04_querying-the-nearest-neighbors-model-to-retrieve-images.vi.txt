[NHẠC] Bây giờ chúng ta có thể sử dụng mô hình. #use image retrieval model with deep features to find similar images. Chúng ta bắt đầu nhé. Chúng ta sẽ lấy mô hình đó và sử dụng nó để tìm các hình ảnh tương tự. Để tôi tiếp tục thực hiện một cách chính xác điều đó. Đầu tiên hãy nhớ lấy một hình ảnh cụ thể. Hãy lấy và chúng ta sẽ gọi chú mèo này, bạn sẽ thấy hình ảnh con mèo. Trong dữ liệu huấn luyện hình ảnh của tôi. Tôi sẽ lấy hình ảnh thứ 18. Đây là hình ảnh thứ 18. Và chỉ để bạn biết đó là gì, tôi sẽ nhập cat['image'].show để hiển thị hình ảnh đó. Và hiển thị nó trong ghi chú ở đây, tôi sẽ đặt mục tiêu canvas là IPython Notebook. Tất cả những thứ chúng ta đang làm cho đến nay. Đây chúng ta có. Đây là một hình ảnh về một con mèo. Với những hình ảnh về con mèo, khá là nhỏ nhưng nó là hình ảnh về một con mèo. Bạn có thể thử làm. Bây giờ chúng ta có thể tìm thấy nearest-neighbors với con mèo. Hãy thực hiện điều đó. Bây giờ hãy lấy hình ảnh của con mèo và tìm nearest-neighbor của nó. Cái mà tôi có thể làm là lấy hnn_model của tôi và truy vấn nó về hình ảnh con mèo. Sau khi truy vấn, bạn sẽ thấy điều đó cho hình ảnh con mèo, những hình ảnh gần nhất là hình ảnh 384, 6910 và vân vân. Có một chút khó khăn để có được những hình ảnh từ những con số. Khi chúng ta tạo ra điều đó, chúng ta sẽ tạo một hàm trợ giúp cái mà lấy các chỉ số và hiển thị các hình ảnh và chúng ta sẽ có cơ hội để xác định một hàm. Tôi sẽ xác định một hàm puthon mới và nó sẽ được gọi là get_images_ids. Trong cột tham khảo ở đây có ids, một cái ở đây. Chúng ta sẽ lấy lại các hình ảnh. Hãy coi như đầu ra ở đây là, xin lỗi. Đầu ra ở đây sẽ là kết quả của truy vấn. Hãy gọi nó là query_result. Nó là một hamg, tất cả chúng ta sẽ làm với query_results trở lại image_train. Tôi sẽ lấy dữ liệu huấn luyện đó và sẽ thực hiện một bước gọi là filter_by, cái mà chỉ cần
 chọn một tập các hàng của dữ liệu đó. Tôi sẽ chọn nó bằng query_result mà chúng ta đã tạo. Khi tôi hoàn thành điều đó và lựa chọn bằng query_result, quay lại với cột và ở đây là cột reference_label. Query_result['reference_label'] và cái mà chúng ta sẽ làm là mang tất cả hình ảnh trở lại trong ổn đinh id của nó phù hợp với cột reference_label này. Đó là cái mà hàm trợ giúp làm. Tôi không phải nhập nó mọi lúc. Chúng ta có thể tiếp tục và sử dụng nó nhưng tôi sẽ làm một thử nghiệm nhỏ với nó. Hãy xác định hàm này. Chúng ta đã xác định nó. Có các hình ảnh từ ids. Thực sự điều đó có ý nghĩa hơn phải không? Hãy gọi là get_images_from_ids. Hãy tiếp tục thực hiện nó. Nhớ rằng với con mèo chúng ta đã có hình ảnh về con mèo của chúng ta, chỉ cần nói là cat_neighbors. Và cat_neighbors sẽ là, hãy gọi hàm này là get_images_from_ids và giống như đầu vào tôi sẽ lấy hnn_model của tôi 
và truy vấn nó với con mèo như một đầu vào. Ok đây là các con mèo tương tự. Các hình ảnh tương tự và cái mà tôi có thể làm là hiển thị các hình ảnh. cat_neighbors[' image'].show. Chúng ta phải dùng một chút dữ liệu để xem, nhưng đây là con mèo gần nhất, đây là con mèo đầu vào và bạn thấy các con mèo
 gần nhất là tất cả các con mèo và chúng là tất cả các loại giống như thế này. [NHẠC]