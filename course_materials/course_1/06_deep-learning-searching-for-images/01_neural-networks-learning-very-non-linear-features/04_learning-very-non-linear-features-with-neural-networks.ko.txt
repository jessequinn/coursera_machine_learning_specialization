[음악] 좋습니다 지금까지 이미지 특징을 활용해 멋진 신발, 드레스 찾아봤습니다 오늘 사용할 기법은 딥러닝이라고 합니다 그중 신경망이란 걸 씁니다 그전에 데이터 표현법을 설명드릴게요 TF-IDF와 단어주머니(Bag-of-words)
모델을 알아봤는데 이미지라면 어떻게 데이터를 표현해야 할까요? 특징이라 불리는 이게
기계학습의 핵심입니다 일반적으로 기계학습이라고 하면
입력이 주어집니다 분류 작업을 할 텐데 감성분석은 했었죠 문장이 주어지면 분류기 모델에 통과시켜 긍정적, 부정적 감성인지 결정했습니다 이미지 분류에서는 입력인 이미지 픽셀을 통해 분류를 하는 것입니다 이건 제 개인데 래브라도 리트리버인지 다른 종류인지 분류하고자 합니다 특징은 데이터 표현법이며 분류기에 입력됩니다 많은 표현법이 있는데 텍스트에는 단어주머니와 TF-IDF가 있었죠 이미지에는 다양한 표현법이 존재합니다 이 모듈에서 그중 몇 개를 설명합니다 오늘은 신경망에 집중하기로 하죠 데이터의 비선형적 표현법을 제공합니다 분류로 다시 돌아가죠 복습을 좀 해봅시다 선형 분류기는 이런 선을 긋거나 긍정 클래스, 부정 클래스 사이의
선형 결정 경계를 설정합니다 경계는 점수 w0 + w1 * x1 + w2 * x2로 이어지는 수식으로 표현됩니다 점수가 0보다 높은 긍정 클래스와 0보다 낮은 부정 클래스로 나눠집니다 점수 함수가 있으면 긍정과 부정을 나눌 수 있습니다 신경망에서는 이런 분류기를
그래프로 표현합니다 x1, x2에서 d번째 특징 xd까지
각각 노드가 있고 예측하고자 하는 결과값 y 노드가 있습니다 첫 특징인 x1에 간선의 가중치 w1을 곱합니다 x2에는 두 번째 간선 가중치 w2 하는 식으로 해서 xd에 마지막 간선의 가중치 wd 를 곱합니다 마지막 가중치 w0은 특징과 곱하지 않고 1을 곱하므로 맨 위에는 1을 적습니다 가중치 w0에서 wd까지를 특징 x1부터 xd, 계수 1까지 곱하면
점수가 나옵니다 점수가 0보다 크면 결과값이 1이 되고 0보다 작으면 결과값이 0이 됩니다 이건 간단한 단층 신경망입니다 선형 분류기를 단층 신경망으로 나타냈습니다 What can this one layer
neural network represent?
단층 신경망으로 뭘 표현할 수 있죠? 함수 x1 OR x2를 신경망으로 나타낼 수 있나요? 함수를 좀 더 엄밀하게 정의할게요 변수 x1, x2와 결과값 y가 있습니다 몇 가지 가능성이 있죠 x1이 0이고 x2가 2이면
x1 OR x2니까 결과값 y는 이 경우 0가 됩니다 x1가 1이고 x2가 0이면 결과값은 1입니다 x1가 0이고 x2가 1이면 1이고요 마찬가지로 둘 다 1이면 1입니다 마지막 세 행의 값이 0보다 크지만
첫 행에서는 0보다 작은 점수 함수를 정의하고자 합니다 어떻게 해야 할까요? 사실 아주 다양한 방법이 있지만 가중치 1을 각 간선 x1, x2에 매기고 점수를 보면 첫 번째 행의 점수는 0이고
다른 행들의 점수는 0보다 큽니다 분리해야 하니 음수를 첫 번째 간선에 할당합니다 -0.5라고 하고 점수가
어떻게 변화하는지 보죠 x1가 0이고 x2가 0일 때 점수는 -0.5이니 결과는 0보다 작습니다
점수도 맞고 결과값도 맞네요 x1가 1이고 x2가 0이면
점수는 0.5입니다 x1가 0이고 x2가 1이여도 같고요 마지막으로 둘 다 1이면 점수는 1.5입니다 간선 가중치만 가지고 OR 함수를 표현했습니다 AND 함수도 표현할 수 있나요? 비슷하게 가중치 1을 각각 간선 x1와 x2에 주는데 이 경우 x1와 x2가 둘 다 1일 때만
1이 돼야 하므로 가장 윗 간선에 -0.5 대신 -1.5을 줍니다 똑같은 방식으로 표를 채워보면 신경망을 통해 AND 함수가 표현됨을 확인할 수 있습니다 단층 신경망은 지금까지 배운 보통의 선형 분류기와 같습니다 선형 분류기가 표현하지 못하는 게 뭐지요? OR는 가능하다고 했죠 AND도 가능했죠 표현 못하는 간단한 함수가 있나요? 예제 하나 드릴게요 선 하나만으로 플러스, 마이너스를
분리하는 건 불가능합니다 이 함수는 XOR라고 합니다 저는 모든 것에 대한 반례라고 부르는데요 반례를 찾고 싶을 때 제일 먼저 XOR를 대입해 봅니다 이 경우 선형적 특징으론 충분하지 않아서 비선형적 특징을 찾아야 합니다 신경망이 활약을 하게 되는데
그 예를 지금부터 보죠 XOR이 뭔지부터 복습할까요 XOR는 x1이 참이고 x2가 거짓이거나 NOT x2이거나 NOT x1일 때 x1이 거짓, 또는 0이며 x2가 1일 때 1이 됩니다 신경망으로 어떻게 표현할까요? 첫 번째 항을 z1, 두 번째 항을 z2라고 합시다 이제 신경망에서 직접 입력 x1, x2를 통해 y를 예측하는 게 아니라 중간값인 z1, z2부터 구한 다음
이를 통해 y를 예측합니다 z1부터 보죠 z1만을 예측하는 신경망을 어떻게 표현할까요 좀 전에 설명드렸지만 한번 더 해보죠 AND와는 조금 다릅니다 반대값을 취해야 하니 x2에 -1을 주고 x1에는 1, 여기는 0.5를 줍니다 z1를 이렇게 표현할 수 있습니다 z2에는 간선 x1에 마이너스 기호를 붙이고 x1에서 z2로 가는 이 간선입니다
간선 x2-z2에 +1를 준 다음 상수 간선에는 -0.5을 주면 z2이 완성됩니다 마지막으로 z1과 z2가 있으면
그 둘을 OR하기만 하면 됩니다 불 변수에 OR 하는 건 이미 알죠 1, 1에 0.5를 빼줍니다 의미 있는 순간입니다 첫 번째 신경망을 만들어 봤습니다 2층밖에 없어 아주 깊지는 않지만
그래도 놀랍습니다 신경망을 처음으로 만들어 봤고요 2층 신경망이었는데 일반적으로 신경망에는 데이터 변환을 위한 수많은 층들이 존재합니다 이런 변환을 통해 비선형 특징을 찾아내는데 컴퓨터 비전에서 몇몇 예를 볼 겁니다 신경망은 거의 50년 가깝게 우리 곁에 있었으니 기계학습이 생겨난 시기와 비슷합니다 하지만 90년대에 인기가 시들해졌는데 연구자들이 신경망으로 그다지
정확도를 높이지 못했기 때문이죠 10년 전쯤 모든 게 바뀌었는데 두 가지 요인이 등장했기 때문입니다 먼저 데이터가 훨씬 증가해서 신경망에 층을 엄청나게 늘릴 수 있었습니다 층이 늘어나면 데이터가 많아야 훈련이 가능하죠 파라미터가 많으니까요 6천만 파라미터의 신경망을 보게 될 겁니다 그러니 훈련 데이터가 무척 많이 필요하죠 최근 들어 이런 엄청난 데이터를 웹 등 여러 채널을 통해 구할 수 있게 됐습니다 두 번째로 컴퓨팅 자원이 발전하여 딥 신경망이 가능해졌습니다 신경망이 커지고 데이터가 늘어남에 따라 빠른 컴퓨터와 GPU가 필요해졌습니다 GPU는 원래 컴퓨터 게임 그래픽
가속을 위해 설계됐죠 알고 보니 많은 데이터를 처리하는 신경망을 만드는데 안성맞춤이었습니다 GPU와 딥 신경망 덕분에 모든 게 바뀌었습니다 실생활에 큰 영향을 끼쳤죠 [음악]